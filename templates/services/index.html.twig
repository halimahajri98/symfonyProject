{% extends 'base.html.twig' %}

{% block title %}Services index{% endblock %}

{% block body %}
    <h1>Services index <a class="btn btn-primary" href="{{ path('services_new') }}">Ajouter</a></h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Designation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for service in services %}
            <tr>
                <td>{{ service.id }}</td>
                <td>{{ service.designation }}</td>
                 <td>
                    <a  class="btn btn-primary" href="{{ path('services_edit', {'id': service.id}) }}">Modifer</a>
                    {# <a class="btn btn-primary" href="{{ path('services_new') }}">Ajouter</a> #}
                    {# <br><br>
                    {{ include('services/_delete_form.html.twig') }}  #}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
